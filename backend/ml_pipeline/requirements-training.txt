# Only needed for offline dataset download/training -- not a runtime
# dependency of the deployed API (see backend/requirements.txt / Dockerfile).
gdown==5.2.0
datasets==5.0.1
huggingface_hub==1.25.1
