AIRNOW_API_KEY=
NOAA_API_KEY=
ENVIRONMENT=development
# DATABASE_URL intentionally omitted -- app/config.py defaults it to an
# absolute path anchored at backend/wildfire.db. Only set this if you want
# to point at a real Postgres instance (e.g. in production on Render).
